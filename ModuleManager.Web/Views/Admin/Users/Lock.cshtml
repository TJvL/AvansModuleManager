@model ModuleManager.Web.ViewModels.PartialViewModel.UserViewModel

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="myModalLabel">Account bewerken</h4>
</div>

@using (Html.BeginForm())
{
    <div class="modal-body">
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        Weet uw zeker dat uw de volgende gebruiker: @Model.GebruikersNaam wilt blokkeren?

     </div>
    <div class="modal-footer">
        <button class="btn" type="button" data-dismiss="modal">Cancel</button>
        <input class="btn btn-primary" type="submit" value="Blokeer" />
    </div>

}


        <div class="modal fade" id="user-lock" role="dialog" aria-labelledby="ModuleAdd" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-body">
                        Weet uw zeker dat uw de volgende gebruiker: <b>Gebruiker naam</b> wilt blokkeren?
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-default" data-dismiss="modal">Annuleren</button>
                        <button type="button" class="btn btn-danger">Blokkeer</button>
                    </div>
                </div>
            </div>
        </div>
